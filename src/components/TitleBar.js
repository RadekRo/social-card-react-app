import React, { Component } from 'react';

class TitleBar extends Component {
    render() {
        return (
            <React.Fragment>
                Title Bar
            </React.Fragment>
        );
    }
}

export default TitleBar;